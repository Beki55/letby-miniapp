export const registerReviewHandler = (bot) => {
    bot.command("review", async (ctx) => {
        await ctx.reply("Reviews are submitted in the Mini App after a verified purchase.");
    });
};
//# sourceMappingURL=review.handler.js.map